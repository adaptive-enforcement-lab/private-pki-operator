# cert-manager-extensions

![Version: 1.15.0](https://img.shields.io/badge/Version-1.15.0-informational?style=flat-square) ![Type: application](https://img.shields.io/badge/Type-application-informational?style=flat-square) ![AppVersion: 1.15.0](https://img.shields.io/badge/AppVersion-1.15.0-informational?style=flat-square)

Private PKI trust root — selfsigned bootstrap, root CA certificate, root CA ClusterIssuer, cluster intermediate CA certificate, and cluster intermediate CA ClusterIssuer

## Maintainers

| Name | Email | Url |
| ---- | ------ | --- |
| Mark Cheret | <mark@cheret.de> | <https://cheret.tech> |

## Values

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| labels.component | string | `"pki"` | Component label |
| labels.environment | string | `""` | Environment label |
| pki.intermediateCA.renewBefore | string | `"168h"` | How long before expiry cert-manager begins attempting renewal |
| pki.intermediateCA.validity | string | `"2160h"` | Cluster intermediate CA validity duration (ISO 8601 duration string) Default: 90 days. Short-lived; cert-manager renews automatically. |
| pki.namespaceCA.chainOfCustody.validationFailureAction | string | `"Audit"` | Kyverno validationFailureAction for pki-namespace-ca-chain-of-custody. Audit only: do not set to Enforce until pki-platform#353 confirms the backfill is clean across all 5 PKI-managed namespaces; the flip itself is a separate, explicit decision. |
| pki.namespaceCA.label | string | `"pki-managed"` | The namespace label this policy treats as its opt-in trigger. Closed boolean set: only the value "true" is accepted. |
| pki.namespaceCA.renewBefore | string | `"168h"` | How long before expiry cert-manager begins attempting renewal |
| pki.namespaceCA.validity | string | `"2160h"` | Namespace intermediate CA validity duration (ISO 8601 duration string). Default: 90 days, matching pki.intermediateCA. Short-lived; cert-manager renews automatically. |
| pki.policies.denyStaging.validationFailureAction | string | `"Audit"` | Kyverno validationFailureAction for pki-deny-staging-configmap-mount. Audit: log violations without blocking (safe default for rollout). Enforce: block pods that mount the staging trust-anchor ConfigMap. |
| pki.policies.enabled | bool | `true` | Deploy Kyverno audit ClusterPolicies for PKI compliance observability. Requires Kyverno to be installed in the cluster. |
| pki.rootCA.renewBefore | string | `"720h"` | How long before expiry cert-manager begins attempting renewal |
| pki.rootCA.validity | string | `"17520h"` | Root CA certificate validity duration (ISO 8601 duration string) Default: 2 years. Root lives online in a Kubernetes Secret and renews automatically — shorter validity bounds blast radius without any operational cost. |
| pki.trustManager.enabled | bool | `true` | Deploy the private-intermediate-ca trust-manager Bundle. When true, trust-manager distributes the intermediate CA certificate to all namespaces as a ConfigMap (key: ca.crt). Note: the private-root-ca Bundle is operator-managed (not chart-managed). |

----------------------------------------------
Autogenerated from chart metadata using [helm-docs v1.14.2](https://github.com/norwoodj/helm-docs/releases/v1.14.2)
