suite: ClusterIssuer / private-intermediate-ca
templates:
  - features/intermediate-ca/cluster-issuer-intermediate-ca.yaml
tests:
  - it: renders a ClusterIssuer
    set:
      labels.environment: qac
    asserts:
      - isKind:
          of: ClusterIssuer
  - it: has the correct name
    set:
      labels.environment: qac
    asserts:
      - equal:
          path: metadata.name
          value: private-intermediate-ca
  - it: references the hardcoded intermediate CA secret
    set:
      labels.environment: qac
    asserts:
      - equal:
          path: spec.ca.secretName
          value: private-intermediate-ca
  - it: carries the environment label
    set:
      labels.environment: dev
    asserts:
      - equal:
          path: metadata.labels.environment
          value: dev
